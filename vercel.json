{
  "version": 2,
  "name": "gocalma-redactor",
  "buildCommand": "npm run setup && npm run build",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

